# 🚀 ScreenOCR Tauri - 快速启动指南

## 📦 第一步：安装依赖

### 1. 确保已安装必要工具

#### ✅ Node.js (v18+)
```bash
node --version
```
如果没有，访问: https://nodejs.org/

#### ✅ Rust
```bash
rustc --version
```
如果没有，执行:
```powershell
# Windows PowerShell (管理员)
winget install Rustlang.Rustup
```

### 2. 安装项目依赖

```bash
cd E:\code\screenocr-tauri
npm install
```

**如果遇到网络问题**，使用淘宝镜像：
```bash
npm install --registry=https://registry.npmmirror.com
```

## 🎯 第二步：启动开发服务器

```bash
npm run tauri:dev
```

**首次启动说明：**
- ⏰ 首次编译需要 3-5 分钟（编译 Rust 依赖）
- 📥 会自动下载 Rust crates
- 🔄 后续启动会快很多（10-20秒）

**成功启动后：**
- ✅ 会自动打开设置窗口
- ✅ 系统托盘会出现图标
- ✅ 左键点击托盘图标可打开设置

## 🛠️ 第三步：开发

### 修改前端代码
- 编辑 `src/` 目录下的文件
- 修改会自动热重载
- 打开浏览器开发工具：F12

### 修改后端代码
- 编辑 `src-tauri/src/` 目录下的文件
- 需要重启开发服务器（Ctrl+C 后重新运行）

## 📦 第四步：构建生产版本

```bash
npm run tauri:build
```

**构建结果：**
- 📁 位置：`src-tauri/target/release/`
- 💿 Windows 安装包：`src-tauri/target/release/bundle/`
- ⚡ 可执行文件：`screenocr-tauri.exe`

## 🎨 当前功能

### ✅ 已实现
- 💎 现代化设置界面
- 🎯 OCR 引擎选择
- ⏱️ 触发延时配置
- ⌨️ 热键设置
- 📋 自动复制开关
- 🖥️ 系统托盘
- 👆 左键点击托盘打开设置

### 🚧 待实现（按规划文档）
- 📸 屏幕截图功能
- 🔍 Tesseract OCR 集成
- ⌨️ 全局热键监听
- 📝 文本识别和复制
- 🎯 WeChatOCR 集成

## 🐛 常见问题快速解决

### 问题1: `npm install` 很慢
```bash
npm install --registry=https://registry.npmmirror.com
```

### 问题2: Rust 编译错误
```bash
rustup update
cd src-tauri
cargo clean
```

### 问题3: 缺少 WebView2（Windows）
- Windows 10/11 通常已自带
- 如果没有，访问：https://developer.microsoft.com/microsoft-edge/webview2/

### 问题4: 缺少 Visual Studio Build Tools
- 下载：https://visualstudio.microsoft.com/downloads/
- 安装 "Desktop development with C++"

## 📚 有用的命令

```bash
# 启动开发服务器
npm run tauri:dev

# 构建生产版本
npm run tauri:build

# 只启动前端（不含 Tauri）
npm run dev

# 清理构建缓存
cd src-tauri
cargo clean

# 更新 Rust
rustup update

# 更新依赖
npm update
```

## 🎯 下一步开发

根据 `TAURI_PROJECT_PLAN.md`，按优先级实现：

### P0 - 必须（Week 1-2）
1. ✅ 基础项目结构
2. ✅ 配置管理
3. ✅ 设置 UI
4. ✅ 系统托盘
5. ⏳ 屏幕截图模块
6. ⏳ Tesseract OCR

### P1 - 重要（Week 3-4）
1. 全局热键
2. OCR 覆盖层 UI
3. 文本选择和复制
4. WeChatOCR 集成

### P2 - 可选（Week 5+）
1. 多显示器支持
2. OCR 结果历史
3. 翻译功能
4. 自动更新

## 🔗 参考文档

- **完整文档**: `PROJECT_CREATED.md`
- **详细规划**: `TAURI_PROJECT_PLAN.md`
- **项目说明**: `README.md`

## ✨ 提示

1. 开发时打开浏览器开发工具（F12）查看控制台
2. Rust 错误会在终端显示
3. 修改前端代码自动热重载
4. 修改 Rust 代码需要重启
5. 首次编译很慢是正常的

---

**祝您开发顺利！** 🎉

如有问题，查看 `PROJECT_CREATED.md` 中的详细说明。

